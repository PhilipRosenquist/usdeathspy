# Changelog

<!--next-version-placeholder-->

## v0.0.1 (13/01/2026)

- First release of `template_package`!

## v0.0.2 (02/17/2026)

- First release of `usdeathspy`!
- Added `get_html_page` to fetch and parse CDC HTML pages
- Added `parse_file_size_mb` to convert file size strings to megabytes
- Added `scrape_mult_mort_user_guide` to scrape CDC Mortality Multiple Cause-of-Death user guide links
- Added `scrape_all_sections` to scrape all CDC Vital Statistics sections into a single DataFrame
- Added `load_cdc_data` to load the pre-scraped CDC Vital Statistics dataset bundled with the package
- Bundled `cdc_all_data.parquet` with 506 rows of pre-scraped CDC Vital Statistics download links
